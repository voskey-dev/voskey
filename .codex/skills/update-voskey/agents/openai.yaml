interface:
  display_name: "Update Voskey"
  short_description: "voskeyをMisskey最新リリースへ追従してDocker更新"
  default_prompt: "Use $update-voskey to merge the latest Misskey release into this voskey checkout, update the version string, and build the tagged Docker image."
